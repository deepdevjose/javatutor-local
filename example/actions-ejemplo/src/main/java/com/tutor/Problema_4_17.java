package com.tutor; // O tu paquete específico

import java.util.Scanner;

public class Problema_4_17 { // El nombre del archivo debe ser Problema_4_17.java

    /**
     * TAREA DEL ESTUDIANTE: Completa el método 'main' para resolver el ejercicio.
     * El programa debe:
     * 1. Leer las dimensiones M y N.
     * 2. Leer los elementos de la matriz A (de M x N).
     * 3. Calcular la matriz transpuesta T (de N x M).
     * 4. Imprimir la matriz transpuesta T.
     */
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        // ----- INICIA CÓDIGO DEL ESTUDIANTE -----

        // 1. Pide y lee las dimensiones M (filas) y N (columnas).

        // 2. Crea la matriz original A[M][N].

        // 3. Usa bucles anidados para leer los elementos de la matriz A.

        // 4. Crea la matriz transpuesta T[N][M].

        // 5. Usa bucles anidados para calcular la transpuesta.
        //    La lógica clave es: T[j][i] = A[i][j];

        // 6. Usa bucles anidados para imprimir la matriz transpuesta T.

        // ----- TERMINA CÓDIGO DEL ESTUDIANTE -----

        scanner.close();
    }
}